<% if book.errors.any? %>
	<div class="row error-box">
		<h5 class="col-xs-12 error-title"><%= book.errors.count %> errors prohibited this book from being saved:</h5>
		<div class="col-xs-12 error-box-text">
			<ul>
				<% book.errors.full_messages.each do |message| %>
				<li class="error-text"><%= message %></li>
				<% end %>
			</ul>
		</div>
	</div>
<% end %>
